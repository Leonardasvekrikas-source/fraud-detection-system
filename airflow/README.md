# airflow/ — Phase 3 (not built yet)

**Planned:** a retraining DAG (ingest → retrain → evaluate vs. live model → promote only if
better), shipped as a **local docker-compose** setup — not a 24/7 server. Placeholder; no DAG
code here yet.
