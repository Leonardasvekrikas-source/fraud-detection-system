# experiments/ — Phase 2 (not built yet)

**Planned, tracked in MLflow:**
- the fusion comparison (feature-level vs decision-level) as a clean, reproducible experiment;
- operating-point analysis: threshold θ as a **cost** decision (false-positive friction vs
  fraud loss), not just a PR curve;
- the PaySim run as second-dataset (generalization) evidence.

Placeholder — no experiment code here yet. Result summaries currently live in `docs/`.
